version: 0.2

phases:
  install:
    commands:
     
  pre_build:
    commands:
      - docker build -t testing ./minimal
  build:
    commands:
      - docker images
      - docker run testing -p 8000:8000